# Vision-to-Cart V4 Dependencies
# Install with: pip install -r requirements.txt

# MCP Server & API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Image Processing
Pillow>=10.0.0
rembg>=2.0.56          # Background removal (optional, graceful fallback if absent)
ultralytics>=8.0.0     # YOLOv8 (optional, graceful fallback if absent)

# Vision & Language Models
openai>=1.30.0         # For GPT-4o vision + embeddings (paid tier)

# Web Search
requests>=2.31.0

# Embeddings & Vector Store
sentence-transformers>=3.0.0   # Free local embeddings (all-MiniLM-L6-v2)
chromadb>=0.5.0                # Vector database (free, no server needed)

# Caching & Session Store
redis>=5.0.0           # Optional, graceful fallback to in-memory if Redis unavailable

# Utilities
numpy>=1.24.0
